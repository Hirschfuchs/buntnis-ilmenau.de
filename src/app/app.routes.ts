import { Routes } from '@angular/router';
import { ComingSoonComponent } from './wartung/coming-soon/coming-soon.component';

export const routes: Routes = [{ path: '**', component: ComingSoonComponent }];
